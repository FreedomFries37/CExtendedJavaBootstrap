#include <stdio.h>


int main(int argc, std::String argv[]) {


	std::File file = access_file("output.txt");

	std::FileWriter fw = new std::FileWriter(file);
	fw->writeln("Hello File!");
	fw->flush();
	fw->close();

	stdout->writeln("What's up its your boy");

	// fw->write("This shouldn't work");


	return 0;
}