


#include "preprocessortesting.h"

#define MAX( X , Y ) ( ((X) > (Y)) ? ( X ) : ( Y ) )
	// t.s = MAX(MAX(1,2), MAX(3,4));

#define CONCAT(X,Y) X##Y


int maxC = CONCAT(MAX,(5,6));

class object {
	virtual public int hashcode() {
		return (int) this;
	}
};



int main(int argc, char* argv[]) {
	int array[][];

	int i = 4;
	void* ptr = &i;


	struct r{
		int s;
	} t;

	//int array[15];

	int other_array[i];

	object o;


	return 0;
}
















