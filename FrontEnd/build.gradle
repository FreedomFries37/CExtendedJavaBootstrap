apply plugin: 'idea'
//and standard one
apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir('src')
        }
    }
}

dependencies {
    implementation project(':Core')
    implementation project(':Input')
}